网关
========

   登录网关：当网关与路由器连接后，可通过网关的ip地址远程登录。如网关的IP地址为 192.168.0.100, 在浏览器中输入 http://192.168.0.100 即可打开网关服务。

网络设置
-----------

概述
   网关同时配备WIFI模块和有线网口，即可通过wifi或有线网与路由器相连。联网后网关可与平台通信,上传用电数据和接收平台下发的控制。
   联网期间需特别留意路由器的DHCP配置，通常家用路由器默认情况为新连接的设备动态分配IP地址，可能发生网关重启后得到了新的IP地址，用户继续访问上次的IP地址将会访问失败。
   当网关被分配了新IP后，可登录路由器页面查找网关设备的当前IP地址。
   对于网关的IP通常做法有:

       #. 网关与路由器连接成功后，在路由器页面的相关设置中，将当前网关的IP地址设置为静态地址.（推荐）
       #. 在网关的配置页面，将自己的IP指定为静态IP，（慎重使用: 当路由器已将相同的IP分配给其它设备时， 会因为IP冲突导致网关将通信失败）。

首次联网
++++++++++

网关联网前，需要进行联网配置，可通过以下方式:
# 有线连接路由器并配置:
# 无线连接路由器并配置:

网关AP:[#ap_limit]_
   网关AP功能主要用于方便电脑/手机打开网关WEB，对网关配置和管理工作。不支持通过AP流量转发，即不支持通过网关AP连接互联网。

   * AP开启[#ap_open]_: 按键位置，参考\ :ref:`网关简介`\ 。按下\ **SET**\ 并持续至少10秒，直到\ **STATE**\ 灯显示\ **红色**\ 常亮后松开按键。
   * 连接AP[#ap_conn]_: 电脑或手机搜索WiFi *ws-agw01-ap* 并连接，密码为 *123456789*。

网关STA: 
   网关STA是网关wifi-sta功能，用于网关连接无线路由器使用。通过sta功能连接至路由器后，网关可以与平台通信，授时任务可以工作。


   新设备配网前，可通过有线连接路由器、启用网关AP的方式实现配网准备，以下示例中假设路由器通过DHCP服务为新连接分配动态地址。

      * 通过有线网络配网：
         #. 用网线将网关与路由器直连，且二者均处于开机状态。
         #. 登录路由器，在“已连接设备”列表中，找到“espress"字样的设备，对应的IP地址即为网关的IP。
            当有多个网关同时联网，可依次打开设备对应的ip地址访问网关，通过页面中显示的SN号码与网关设备一一对应。

      * 通过AP配网: 
         #. 在无网线直连网关与路由器时，长按网关面板上的"SET"按键且持续约10秒，待面板"STATE"灯(红色)常量后松开按键。
         #. 上步操作后，网关已开启了AP，SSID(无线名称)为 “ws-agw01-ap”，密码：123456789。
         #. 打开电脑搜索WIFI，连接网关AP。通过浏览器访问 http://192.168.20.1 即可登录网关WEB页面。

网络配置
++++++++++

   登录网关页面后，可配置网关的联网配置：

   通过WIFI连接路由器:
      ......


   网关联网方式有wifi、有线网络、4G(可选), 联网后可通过访问网关的web本地操作设备管理、平台配置等事项。
   
      *通过按下网关上的\ **SET**\ 按钮，可以开启网关的AP, SSID:ws-agw01-ap, PW:123456789*, 可通过电脑/手机的wifi连接实现访问网关web（http://192.168.20.1）。

      wifi联网: 
      有线网: 
      4G-LTE: 4G模块为网关可选配置，如网关安装了4G模块，可以使用4G通信，仅连接平台并与之通信。

设备配对
---------

    断路器接入电网后，需要与配套网关配对后才能受控于网关，受控内容包含 分合闸、特色功能使能或禁用、告警等临界值的设定，及上述所有数据的查看。

    断路器与网关的配对需要二者通过485总线连接后操作.

    操作步骤:
    1. xx
    #. xx

LED状态汇总
-----------

定时重启
---------
    
    **版本 >= 02_12**

    网关启动后，将每间隔12小时重启一次。

本地
--------

      * 管理
         #. CRUD
         #. 更换(暂无)
         #. 升级

      * 网络
         #. 有线网络
            * DHCP
         #. 无线网络
            * DHCP

      * 控制
         #. 分和闸

      * 异常
         #. 过流、过压、欠压、漏电

---

.. [#ap_limit] 网关AP的应用限制:
   #. 网关AP与STA同时开启，在按下“提交”或者网关启动后可能出现短时(20秒) 网络不稳定，即电脑连接网关AP时可能出现断连或访问速度变慢。
   #. 网关AP与STA同时开启后，网关STA连接路由器wifi 仅尝试3次，全部失败后将不再重连。